import { updateBlog } from "@/db/blog/queries";

export default async function UpdateBlog({params}:{params:object}) {
    // const getID = parseInt(id);
    // await updateBlog(getID,data);
    console.log(params);

    return (<>
        <div className="layout">

        </div>
    </>);
}